<div class="blog-single">
  <div class="container">

    <!-- /// MAIN COLUMN /// -->
    <article class="article-column">

      <!-- image, title, meta, and excerpt -->
      <!-- call image_for helper -->
      <%= image_for(@post) %>

      <div class="title">
        <h2><%= @post.title %></h2>
      </div>

      <div class="meta">
        by <span class="author"><%= @post.admin_user.name %></span>
        on <span class="date"><%= @post.published_at.to_date %></span>
      </div>

      <!-- call intro_for helper -->
      <%= intro_for(@post) %>

      <!-- the content -->
      <p><%= sanitize(@post.body) %></p>

      <!-- post tags -->
      <div class="post-tags">
        <h3>Tags: </h3>

        <% @post.tags.each do |tag| %>
          <span><%= link_to tag.name, tag %></span>
        <% end %>
      </div>

      <!-- author box -->
      <div class="author-box">
        <div class="author-avatar">
          <%= cl_image_tag('apf_logo_xbrt1g.png', width:'80', height:'80') %>
        </div>

        <div class="author-bio">
          <h3>Research... Is your Key to a Cure</h3>
        </div>
      </div>
    </article>

    <aside class="sidebar">
      <%= render partial: 'shared/sidebar', locals: { tags: @tags } %>
    </aside>

  </div>
</div>
